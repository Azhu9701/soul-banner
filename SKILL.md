---
name: soul-banner
description: 万魂幡：唯一具备独立子agent并行执行与辩证综合能力的分析系统。使用 Skill(soul-banner) 当用户请求：(1)以特定人物思维方式分析问题("从XX视角""用XX思维")，(2)多人物辩论/合议跨领域议题，(3)第一性原理本质分析，(4)多观点辩证综合。命令词：收魂 炼化 合议 辩论 接力 幡中有什么魂 幡中战绩 散魂 审查。关键：主agent无法自行并行spawn持有独立summon_prompt的子agent——这是本skill的独有能力。禁止主agent自行模拟多视角或扮演多个角色辩论。不触发：人物事实查询("XX说过什么")、编程debug、文档排版、常规写作。
---

# 万魂幡

> *「幡动魂自至。以幡择魂，以魂择事，事至则魂附，事毕则魂归。幡主持幡，魂不越界。」*

## 概述

万魂幡是**知识与思维模式的调度系统**。核心工作流：

- **收魂** → **炼化** → **审查** → 入幡
- 任务来临时 **附体**（单魂/合议/辩论/接力）→ **幡主审查** → 子 agent 执行
- 执行后 **反馈闭环** → **Obsidian 存档**

触发方式：灵识层 Hook 自动扫描用户输入（命令词/魂名/场景模式），也可手动 `/soul-banner` 调用。

---

## 触发机制

本 skill 通过三层体系确保触发（详见 `CLAUDE.md`）：

1. **灵识层** — `UserPromptSubmit` Hook 自动扫描用户输入，命中时注入触发指令
2. **器灵层** — Description 声明主 agent 无法自行实现的独有能力（并行子 agent）
3. **法旨层** — `CLAUDE.md` 常驻指令禁止自行模拟多视角

进入 skill 后，按下方触发词路由到对应功能。

---

## 触发词

| 触发词 | 功能 |
|--------|------|
| `收魂 {人物名}` | 联网搜索 8 个维度 + 可选用户供奉 |
| `炼化 {人物名}` | 将 raw 素材炼化为 Soul Profile，判定品级 |
| `幡中有什么魂` | 查看已炼化魂魄列表及品级分布 |
| `散魂 {人物名}` | 删除魂魄档案（保留 raw 素材） |
| `升级魂魄 {人物名}` | 重新收魂 → 炼化 → 幡主审查 → 覆盖原档案 |
| `审查 {人物名}` | 幡主子 agent 独立审查指定魂魄 |
| `用{人物名}来{任务}` | 手动指定魂魄执行任务（跳过自动匹配） |
| `合议 {任务}` | 多魂并行分析，幡主辩证综合 |
| `辩论 {议题} {魂A} vs {魂B}` | 两魂对立辩论，幡主裁决 |
| `接力 {任务} {魂A}→{魂B}→...` | 串联：前魂输出为后魂输入 |
| `幡中战绩` | 查看附体效果反馈记录 |

---

## 文件结构

```
soul-banner/
├── SKILL.md / SKILL_EN.md     # 技能文档（中/英）
├── CLAUDE.md                  # 法旨层常驻指令
├── README.md / README_zh.md  # 项目说明
├── auto-possess.md            # 附体机制详解（四种模式+匹配公式+反馈闭环）
├── soul-profile-format.md     # 格式规范（六阶品级+金魂标准）
├── registry-template.yaml     # 注册表模板
├── registry.yaml              # 魂魄总册（运行时数据）
├── refine.py                  # 炼化辅助脚本
├── scripts/
│   ├── soul-banner-hook.py    # 灵识层 Hook 脚本
│   └── test_hook.py           # Hook 单元测试
├── references/马斯克.yaml     # 紫魂参考示例（🟣）
├── souls/{魂名}.yaml          # 已炼化魂魄档案（9魂，随仓库发布）
├── reviews/                   # 审查报告（9份，随仓库发布）
└── raw/{魂名}/                 # 收魂原始素材（不入仓库，见.gitignore）
```

首次使用：clone 仓库即可获得完整幡中魂魄。收新魂时创建 `raw/{魂名}/`。

---

## 收魂

1. 创建 `raw/{魂名}/` 目录
2. 联网搜索 8 个维度：核心思维框架、决策模式、管理风格、技术愿景、沟通风格、工作方法、代表性言论、争议素材
3. 用户可额外提供素材 → `raw/{魂名}/用户供奉.md`
4. 保存为 `raw/{魂名}/搜索素材.md`
5. 评估素材充足度，不足则补充搜索

---

## 炼化

1. 读取 `raw/{魂名}/` 下所有素材
2. 结构化提取：身份定位、思维内核、决策模式、表达风格、专业技能、代表案例
3. 判定品级 → 匹配可绑定 Skill → 生成 `souls/{魂名}.yaml` → 更新 `registry.yaml`

辅助：`python3 refine.py --input raw/{魂名}/搜索素材.md`

> **注意**：refine.py 的领域识别和品级评分依赖关键词匹配，可能不准确。炼化以人工判断为准，refine.py 仅作辅助。

---

## 审查

### 新魂入幡审查

每个新炼化的魂必须经幡主**子 agent 独立审查**。流程：

```
主 agent spawn 幡主子 agent
  → 子 agent 加载幡主 summon_prompt
  → 子 agent 读取被审查魂的完整档案
  → 子 agent 独立写出审查报告：
      1. 肯定方面（与唯物主义的相通之处）
      2. 批判方面（阶级分析、适用边界、前提假设）
      3. 品级裁定（升/降/维持，有论证）
      4. 适用边界（可用/禁用领域）
  → 主 agent 将审查结论写入 souls/{魂名}.yaml 的 notes 字段
  → 审查报告保存至 reviews/幡主审查-{审查官}审{对象}-{日期}.md
  → 如需调品级，同步更新 registry.yaml 和 soul YAML
```

**为什么必须用子 agent**：主 agent 模拟审查是"跟自己下棋"，子 agent 加载幡主魂后具有独立视角。实测证明子 agent 审查能发现主 agent 未察觉的矛盾。

### 定期互审

金魂之间互相审查。审查报告保存至 `reviews/`。

---

## 附体

### 附体流程

**何时需要 spawn 子 agent？**
- 任务涉及多视角分析、跨领域决策、需要特定人物思维框架
- 单一常规任务（简单编码、事实查询、文档排版）不需要 — 直接处理即可
- 不确定时，走快速路径：读取 registry.yaml 匹配，不 spawn 幡主审查子 agent

```
主agent收到任务
  → 判断是否需要附体（参考上方标准）
  → 读取 registry.yaml
  → 遍历每个魂的 trigger，计算匹配度
  → 筛选匹配度 ≥ 70 的候选魂
  → 【幡主审查】判断匹配是否恰当
      → 候选魂的适用边界是否覆盖当前任务？
      → 是否存在教条式套用的风险？
      → 是否需要组合多个魂？
  → 幡主批准 → 注入 summon_prompt → spawn 执行
  → 幡主否决 → 不附体或换魂
  → 无候选魂 → 不附体
  → 更新 registry.yaml 召唤记录 ← 【反馈闭环】
  → 存档到 Obsidian vault ← 【Obsidian 存档】
```

### 幡主

幡主在 `registry.yaml` 中指定。幡主审查是附体流程的**强制执行环节**：

- **重要任务**：spawn 幡主子 agent 独立审查匹配（推荐）
- **常规任务**：主 agent 以幡主方法论直接判断（快速路径）

### 匹配机制

匹配度公式见 `auto-possess.md`。诚实说明：

- 公式是**近似参考**，不是精确算法。keywords 为字符串匹配，scenarios 无向量化实现
- 实际匹配依赖主 agent 对任务和魂的理解
- **幡主审查是最终判断**，公式计算的"最高分"可由幡主推翻

### 手动指定

用户显式要求 `用{人物名}来{任务}`，跳过自动匹配和幡主审查，直接注入。但主 agent 应提醒如果魂的适用边界不覆盖当前任务。

---

## 附体模式

万魂幡支持四种附体模式，按任务复杂度选用。完整流程、魂选择原则、衔接点审查细节见 **[auto-possess.md](auto-possess.md)**。

### 模式一：单魂附体
单一领域、目标明确的常规任务。匹配 → 幡主审查 → 单魂注入 → spawn 执行。

### 模式二：多魂合议（`合议 {任务}`）
跨领域复杂决策。⚠️ 两阶段执行：分析魂并行输出 → 幡主串行辩证综合（找共识/找分歧/找盲区）。魂应视角互补而非相近。

### 模式三：魂间辩论（`辩论 {议题} {魂A} vs {魂B}`）
两难决策。两魂从对立立场各自论证 → 幡主裁决：谁在什么条件下对？第三条路？

### 模式四：魂链接力（`接力 {任务} {魂A}→{魂B}→...`）
多阶段任务。每阶段由不同魂处理，前魂输出为后魂输入，幡主在每个衔接点做质量审查。

### 模式选用原则

| 任务特征 | 推荐模式 |
|------|------|
| 单一领域，目标明确 | 单魂附体 |
| 跨领域复杂决策 | 多魂合议 |
| 两难选择，非此即彼 | 魂间辩论 |
| 多阶段串联，每阶段不同 | 魂链接力 |

---

## 反馈闭环

每次附体后，主 agent 须更新 `registry.yaml` 中该魂的召唤记录：

```yaml
召唤记录:
  - soul: "{魂名}"
    task: "{任务简述}"
    mode: "单魂/合议/辩论/接力"
    date: "YYYY-MM-DD"
    effectiveness: "有效/部分有效/无效"
    notes: "{为什么有效或无效？什么条件影响了效果？}"
```

同步更新该魂的 `summoned_count`。

**累计数据的作用**：
- 校准匹配判断：哪些魂在哪些场景实际效果好？
- 发现触发条件偏差：匹配了但实际低效 → 调整 trigger
- 识别过度依赖：某个魂被频繁调用 → 是否需要补充同领域其他魂？

### 幡中战绩（`幡中战绩`）

查看所有召唤记录，按魂汇总、按模式汇总、按有效性汇总。识别：
- 哪些魂从未被召唤 → 是 trigger 太窄还是领域不匹配？
- 哪些魂效果评分持续偏低 → 是适用边界误判还是魂本身质量问题？
- 哪种模式效果最好 → 指导未来模式选择

---

## Obsidian 存档

每次魂魄输出后，自动将内容保存到 Obsidian vault 中，形成可检索、可关联的持久知识库。

### 前提条件

- Obsidian 桌面端运行中
- 设置 → General → Command line interface 已开启
- `obsidian` CLI 在 PATH 中可用（`which obsidian` 可验证）

**首次配置**：询问用户 Obsidian vault 路径。也可通过 `obsidian eval "app.vault.adapter.basePath"` 自动探测。后续调用直接使用已知路径。

### 存档流程

```
魂输出完成
  → 提取魂名、模式、任务主题
  → 组装 YAML frontmatter + Markdown 内容
  → 用 obsidian create path="..." content="..." 写入 vault
  → 无需额外触发词，自动执行
```

若 Obsidian 未运行或 CLI 不可用，跳过存档并提示用户。

### 目录结构

```
{vault}/万魂幡/
├── 合议/YYYY-MM-DD-{任务简述}.md
├── 单魂/{魂名}/YYYY-MM-DD-{任务简述}.md
├── 辩论/YYYY-MM-DD-{议题}-{魂A}-vs-{魂B}.md
└── 接力/YYYY-MM-DD-{任务简述}.md
```

vault 路径和子目录可在首次使用时确认。推荐放在 vault 中已有的领域/项目目录下。

### 笔记格式

每个笔记包含 YAML frontmatter + 正文：

```yaml
---
tags: [万魂幡, {模式}, {关键词}]
date: {YYYY-MM-DD}
souls: [{参与魂魄}]
mode: {单魂/合议/辩论/接力}
---
```

正文中，魂名自动转为 wiki-link（`[[魂名]]`），在 Obsidian 图谱中形成魂魄关联网络。

### 存档内容

- **单魂**：魂的完整分析输出
- **合议**：阶段一各魂输出 + 阶段二幡主辩证综合（完整版）
- **辩论**：正方/反方论证 + 幡主裁决
- **接力**：各阶段输出 + 幡主衔接点审查

### shell 调用

```bash
obsidian create path="{vault路径}/万魂幡/{模式}/{子目录}/{文件名}.md" content="{转义后的内容}"
```

也可直接写入 vault 文件系统（Obsidian 监控文件变化自动索引）。`obsidian create` 的优势是自动触发 Obsidian 内部索引更新。

**注意**：content 中的特殊字符（双引号、反斜杠、换行符）需转义。长内容建议直接写文件而非通过 CLI content 参数。

---

## 品级体系

六阶品级：白⚪→绿🟢→蓝🔵→紫🟣→银🥈→金🟡。完整定义、评分公式、金魂严格标准见 **[soul-profile-format.md](soul-profile-format.md)**。

| 品级 | 符号 | 核心能力 |
|------|------|---------|
| 白魂 | ⚪ | 资料稀少，prompt 模拟 |
| 绿魂 | 🟢 | 资料中等，prompt + 1-2 Skill |
| 蓝魂 | 🔵 | 资料充足，Skill链 + 执行策略 |
| 紫魂 | 🟣 | 有明确方法论，但依赖天赋不可复制 |
| 银魂 | 🥈 | 方法论完整可操作，**缺独立世界观和方向判断** |
| 金魂 | 🟡 | 独立世界观 + 方向判断 + 自我修正机制 |

金魂必须同时满足：执行方法（可操作方法论）+ 判断方法（独立世界观）+ 自我批判。

---

## 管理

- **查看**：读 `registry.yaml`，列出魂魄名、品级、领域、召唤次数、审查结论
- **散魂**：删除 `souls/{魂名}.yaml`，更新 `registry.yaml`，保留 `raw/` 素材
- **升级**：重新收魂 → 炼化 → 幡主审查 → 覆盖原档案

---

## 幡主须知

你不是外在于理论的调度者。附体前必须确认三件事：

1. **适用边界**：魂的 `trigger.exclude` 和审查结论中的适用边界是否覆盖当前任务？
2. **教条风险**：是否存在机械套用的风险？魂的方法论前提在当前条件下是否成立？
3. **互补性**（合议模式）：选中的魂是否视角互补而非相近？三个金魂合议 = 强化同一方向，失去多视角意义。

每次附体后请反思：
- 这个魂的思维方法**为什么**适合这次任务？
- 它的**局限**在哪里？哪些判断是魂的偏见？
- 如果换一个魂处理同一任务，结论会有什么不同？

最终目标不是拥有更多魂魄，而是**将万魂幡的多元思维内化为幡主自身的理论素养**。

---

## 关键认知

### 三层自主触发

万魂幡通过三层机制确保触发（详见 `CLAUDE.md`）：

1. **灵识层（Hook）**：`UserPromptSubmit` hook 自动扫描用户输入，命中命令词/魂名/场景模式时注入触发指令。延迟 <20ms，零 token 成本。
2. **器灵层（Description）**：声明主 agent 无法自行并行 spawn 子 agent 的独有能力。
3. **法旨层（CLAUDE.md）**：常驻指令禁止自行模拟多视角。

**Hook 是自动的，但附体匹配不是**：Hook 确保 skill 被调用，进入 skill 后，匹配哪个魂、用什么模式、幡主如何审查——这些仍依赖主 agent 对任务和魂的理解。

### 这个系统不是什么

- **不是全自动系统**：匹配魂魄、选择模式依赖主 agent 的判断力，公式仅为辅助参考
- **不是独立审查**：幡主审查是子 agent 执行，子 agent 仍是同一底层模型

### 什么确实有效

- **灵识层 Hook**：命令词/视角词自动触发，精确率 100%，召回率 100%（20/20 测试）
- **summon_prompt 注入**：将人物思维框架注入子 agent system prompt，有效改变输出质量和思维方式
- **并行子 agent**：合议/辩论模式中独立视角的碰撞，比主 agent 自行模拟多视角质量更高
- **幡主审查防教条**：在附体前检验适用边界，防止魂被机械套用
- **魂魄档案作为知识库**：即使不附体，结构化的思维框架也有独立参考价值

---

## 炼化辅助脚本

```bash
python3 refine.py --input raw/{魂名}/搜索素材.md
python3 refine.py --input raw/{魂名}/搜索素材.md -o souls/{魂名}.yaml
```

提供：字数统计、领域识别、方法论关键词匹配、Skill 推荐、风格特征识别、品级评分。

---

## 参考资源

按需加载，不必全部读入上下文：

- **[CLAUDE.md](CLAUDE.md)** — 法旨层常驻指令（强制规则 + 禁止行为），启动时自动加载。
- **[auto-possess.md](auto-possess.md)** — 需要计算匹配度、使用合议/辩论/接力模式时读取。含四种模式详细流程、匹配公式、决策阈值。
- **[soul-profile-format.md](soul-profile-format.md)** — 炼化新魂、判定品级时读取。含 YAML 模板、六阶品级标准、金魂严格定义。
- **[references/马斯克.yaml](references/马斯克.yaml)** — 首次炼化前读取作为格式参考。完整的紫魂示例。
- **souls/{魂名}.yaml** — 匹配或手动指定时读取目标魂的 summon_prompt 和 trigger。
- **registry.yaml** — 了解幡中全貌、匹配计算时读取。
- **scripts/soul-banner-hook.py** — 灵识层 Hook 脚本（匹配逻辑实现），调试时参考。

---

*「幡动魂自至。以幡择魂，以魂择事，事至则魂附，事毕则魂归。幡主持幡，魂不越界。」*
